Test FW: Wireless RX
====================

This project receives a `COM_PL_SIZE`-byte packet and sets the debug LEDs based
on its first 10 bytes, delaying between each setting.

The address is fixed at `0xAAAA01`.
